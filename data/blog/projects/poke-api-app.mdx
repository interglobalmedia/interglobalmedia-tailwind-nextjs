---
title: Poke API App
date: '2022-07-01'
lastmod: '2022-07-01'
tags:
  [
    'ajax',
    'create element',
    'css transforms',
    'fetch api',
    'nested map methods',
    'nested api objects',
    'poke api',
    'promise chaining',
  ]
categories: ['manipulating the dom', 'project']
draft: false
summary: An application which utilizes the Poke API based on Pokemon characters and utilizes both the fetch API and AJAX, JavaScript promise chaining, the map method, creates most elements rendered to the html pages, checks for non-existent images and removes them or or sets their display properties to none, uses pointer events to replace touch and mouse events, custom build script, and Modern JavaScript in general.
images: ['/static/images/projects/img/webp/poke-api.webp']
layout: PostLayout
canonicalUrl: /blog/projects/poke-api-app
---

## Description

An `application` which **_utilizes_** the `Poke API` **_based_** on `Pokemon` **_characters_** and **_utilizes_** both the `fetch API` and `AJAX`, `JavaScript promise chaining`, the `map method`, `creates` **_most elements_** `rendered` to the `html pages`, **_checks_** for **_non-existent_** `images` and **_removes_** them or or **_sets_** their `display properties` to `none`, **_uses_** `pointer events` to **_replace_** `touch` and `mouse events`, `custom build scripts`, and `Modern JavaScript` in **_general_**.

### Project Includes

`ajax`, `animated magnifying glass icon search input`, `code refactor`, `create element`, `css transforms`, `css transitions`, `design refactor`, `fetch api`, `innerhtml property`, `keydown event`, `load event`, `manipulating the dom`, `map method`, `mobile friendly`, `modern javascript`, `nested map methods`, `nested api objects`, `node sass`, `onerror property`, `parallax`, `pointer events`, `poke api`, `promise chaining`, `window pageOffset`, `window scroll`

- [View Poke API on Github GH Pages](https://interglobalmedia.github.io/pokemon-api/)
